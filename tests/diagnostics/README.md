# Frame Metrics Tests

These tests verify bounded frame-statistic calculations in `src/diagnostics/`
without DOM rendering. Browser layout and integration are covered by the
[existing browser checks](../browser/README.md); actual Windows-PCVR performance
requires installation evidence.
