/**
 * Purpose: Author the raptor the heat view reveals over the landscape.
 * Context: The ladder carries a module forward unchanged once it is introduced.
 * Responsibility: Own the one copy of these values.
 * Boundary: Data only; no runtime resources and no level presentation.
 */

import type { RaptorPreset } from "../../modules/raptor/raptor";

export const RAPTOR: RaptorPreset = {
  // The fur colour the walking population carries. Seventy metres up, the
  // bird is far outside the heat view's reach and reads in the echo palette
  // like any body the warmth has not reached.
  color: 0x171717,
};
