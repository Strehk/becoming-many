# Texture Loader

This utility contains shared loading helpers for runtime textures and texture
metadata.

It should centralize only common loading and configuration behavior. Material
choices, visual contracts, and texture ownership remain with the consuming
module.
