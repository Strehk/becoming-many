# Scent Particles

This module contains visible scent signatures and scent-particle effects.

It consumes authored scent data and world-field samples, uses bounded particle
resources, and owns its particle lifecycle. It does not own the master audio
clock or a separate render loop.
