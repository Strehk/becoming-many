# Textures

This folder contains runtime texture assets served unchanged by Vite.

Store only selected, optimized shipping textures here. Source files,
intermediate exports, and authoring material belong outside `public`.
